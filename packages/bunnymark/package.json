{
  "name": "@momoyu-ink/bunnymark",
  "version": "0.1.0",
  "description": "A bunny mark benchmark for Moyu engine.",
  "main": "dist/index.js",
  "module": "src/index.ts",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "vite",
    "prepack": "yarn build",
    "build": "vite build && tsc --emitDeclarationOnly --declarationDir ./dist --rootDir ./src",
    "watch": "vite build --watch"
  },
  "author": "Icemic <bingfeng.web@gmail.com>",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@momoyu-ink/kit": "workspace:*"
  },
  "devDependencies": {
    "@types/node": ">=24",
    "typescript": "^5.6.3",
    "vite": "^8.0.16"
  }
}
