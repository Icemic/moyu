[package]
authors = ["Icemic <bingfeng.web@gmail.com>"]
description = "Platform abstraction layer for Hai."
edition = "2021"
license = "MPL-2.0"
name = "hai_pal"
repository = "https://github.com/Icemic/hai"
version = "0.7.0"

[features]
default = []
desktop = [
  "env_logger",
  "node-resolve",
  "parking_lot",
  "tokio",
  "parking_lot/hardware-lock-elision",
]
web = [
  "console_error_panic_hook",
  "console_log",
  "parking_lot",
  "wasm-bindgen-futures",
  "web-time",
  "web-sys",
]

[dependencies]
anyhow = "1.0"
log = "0.4"
once_cell = "1.16"
parking_lot = {version = "0.12", optional = true}
pollster = "0.3.0"
reqwest = {version = "0.12", default-features = false, features = ["rustls-tls", "gzip", "deflate", "brotli"]}
serde = {version = "1.0", features = ["derive"]}
url = "2.2"
# deps for desktop platforms
env_logger = {version = "0.11", optional = true}
node-resolve = {version = "2.2", optional = true}
serde_json = "1.0"
tokio = {version = "1", features = ["full"], optional = true}
# deps for web
console_error_panic_hook = {version = "0.1", optional = true}
console_log = {version = "1.0", features = ["color"], optional = true}
wasm-bindgen-futures = {version = "0.4", optional = true}
web-sys = {version = "0.3.56", features = ["Window", "Location"], optional = true}
web-time = {version = "1.1", optional = true}

[target.'cfg(target_os = "android")'.dependencies]
android_logger = "0.14.1"
