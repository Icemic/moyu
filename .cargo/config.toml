# Add the contents of this file to `config.toml` to enable "fast build" configuration. Please read the notes below.

# NOTE: For maximum performance, build using a nightly compiler
# If you are using rust stable, remove the "-Zshare-generics=y" below.

[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = ["-Clink-arg=-fuse-ld=lld"]

[target.x86_64-apple-darwin]
linker = "rust-lld"
rustflags = []

[target.aarch64-apple-darwin]
linker = "rust-lld"
rustflags = []

[target.x86_64-pc-windows-msvc]
linker = "rust-lld.exe"
rustflags = []

# Optional: Uncommenting the following improves compile times, but reduces the amount of debug info to 'line number tables only'
# In most cases the gains are negligible, but if you are on macos and have slow compile times you should see significant gains.
[profile.dev]
debug = 1

[build]
# rustc-wrapper = "/usr/local/bin/sccache"

[env]
FFMPEG_DIR = {value = "3rdparty/ffmpeg", relative = true}

[alias]
run-wasm = "run --release --package run-wasm -- moyu --lib --release"
run-desktop = "run --package moyu --features desktop"
# should have "sudo CARGO_PROFILE_RELEASE_DEBUG=true" before `cargo`
run-flamegraph = "flamegraph --package moyu --features desktop"
